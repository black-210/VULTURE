from typing import Any, Dict

def extract_metadata(path: str) -> Dict[str, Any]:
    """Extract metadata from a file or sidecar (placeholder)."""
    # In real implementation, parse SigMF or JSON sidecar
    return {"path": path, "sample_rate": None, "channels": None}
